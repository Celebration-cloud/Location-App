import { Stack } from "expo-router";

export default function RootLayout() {
  const colorTheme = useC
  return <Stack screenOptions={{statusBarBackgroundColor: "yellow", statusBarStyle: "dark"}} />;
}
